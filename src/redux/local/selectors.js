export const selectListBox = state => state.local.listBox;

export const selectId = state => state.local._id;

export const selectPath = state => state.local.path;

export const selectPage = state => state.local.page;

export const selectMail = state => state.local.mail;
